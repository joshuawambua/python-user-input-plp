# python-user-input-plp
name=input("What is ypur name?")
age=input('How old are you?')
location=input('Where do you come from?')
print('Hello '+name+','+'you are '+str(age)+' years old'+'.'+' You are from '+location+'.')
